from django.urls import path, include
from . import views

urlpatterns = [
    # path('email/', views.SubscribeEmail, name='SubscribeEmail'),
    # path('sms/', views.SubscribeSMS, name='SubscribeSMS'),
    # path('publish/', views.PublishNotification, name='PublishNotification'),
]
